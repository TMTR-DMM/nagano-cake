<div class="container mb-4">
  <div class="row">
    <div class="col">
      <div class="w-50 mx-auto">
        <h2 class="mb-4">会員の方はこちらからログイン</h2>
        <%= form_with model: @customer, url: customer_session_path, id: 'new_customer', class: 'new_customer', local:true do |f| %>
        <div class="form-group">
          <%= f.label :email, "メールアドレス" %><br>
          <%= f.email_field :email, autofocus: true, autocomplete: "email", placeholder: "sample@example.com", class: "form-control w-50" %>
        </div>
        <div class="form-group">
          <%= f.label :password, "パスワード" %><br>
          <%= f.password_field :password, autocomplete: "current-password", class: "form-control w-50" %><br>
        </div>
          <%= f.submit "ログイン" %>
        <% end %>
        <h2 class="mb-4 mt-4">会員登録がお済みでない方</h2>
        <p><%= render "devise/shared/links" %>から新規登録を行ってください。</p>
      </div>
    </div>
  </div>
</div>