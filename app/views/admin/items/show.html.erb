<h1>商品詳細</h1>
<%= attachment_image_tag @item, :image, size: '60x60', format: 'jpg' %>
<p>商品名<%= @item.name %></p>
<p>商品説明<%= @item.caption %></p>
<p>ジャンル<%= @item.category.category %></p>
<p>税込価格<%= (@item.excluding_price * 1.08).ceil %>(<%=@item.excluding_price %>)<br>
   (税抜価格)
</p>
<p>販売ステータス
 <% if @item.is_deleted == true %>
    <span>販売中</span>
<% else %>
    <span>販売停止中</span>
<% end %>
</p>

<%= link_to '編集する', edit_admin_item_path(@item) %>