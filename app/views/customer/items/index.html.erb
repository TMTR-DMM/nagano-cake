<h1>商品一覧<span>(全8件)</span></h1>
<div class="d-inline-flex my-box-light flex-wrap">
<% @items.each do |item| %>
<%= link_to item_path(item.id) do %>
<%= attachment_image_tag item, :image, fallback: 'no_image.jpg', size: '80x80', format: 'jpg' %>
<% end %><br>
<%= item.name %><br>
<%= item.excluding_price %>
<% end %>
</div>
<%= paginate @items %>