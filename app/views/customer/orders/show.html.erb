<div class="container">
<h1>注文履歴詳細</h1>
<div class="row">
<div class="col-sm-8">
<p>注文情報</p>
<table class="table" style="width:600px;">
    <tr><td>注文日</td><td><%= @order.created_at.strftime("%Y/%m/%d") %></td></tr>
    <tr><td>配送先</td><td><%= @order.postcode %><br><%= @order.address %><br><%= @order.name %></td></tr>
    <tr><td>支払方法</td><td><%= @order.payment_method %></td></tr>
    <tr><td>ステータス</td><td><%= @order.order_status_i18n %></td></tr>
</table>
</div>
<div class="col-sm-4">
<p>請求情報</p>
<table class="table" style="width:300px;">
    <tr><td>商品合計</td><td><%= @order.amount_charged - @order.postage%></td></tr>
    <tr><td>配送料</td><td><%= @order.postage %></td></tr>
    <tr><td>ご請求金額</td><td><%= @order.amount_charged %></td></tr>
</table>
</div>
</div>


<p>注文内容</p>
<table class="table" style="width:800px;">
    <thead>
        <th>商品</th>
        <th>単価（税込）</th>
        <th>個数</th>
        <th>小計</th>
    </thead>
    <tbody>
        <% @order_items.each do |order_item| %>
        <tr>
            <td><%= order_item.item.name %></td><td><%= (order_item.purchase_price * 1.08).round(0) %></td><td><%= order_item.ordered_qty %></td><td><%= (order_item.purchase_price * 1.08).round(0) * order_item.ordered_qty %></td>
        </tr>
        <% end %>
    </tbody>
</table>
</div>